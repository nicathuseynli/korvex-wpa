# Backend API (server-side only — never exposed to client)
KORVEX_API_BASE_URL=https://api.korvex.app

# Paddle checkout URLs (server-side only — rendered into page HTML)
PADDLE_CHECKOUT_WEEKLY=https://checkout.paddle.com/product/weekly
PADDLE_CHECKOUT_MONTHLY=https://checkout.paddle.com/product/monthly
PADDLE_CHECKOUT_ANNUAL=https://checkout.paddle.com/product/annual
